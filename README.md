`react-native-svg-loader` is meant for loading SVG files into `react-native-svg` components.

```js
<SVGLoader source={require('./image.svg')} />
<SVGLoader source={{uri: 'http://remote/image.svg'}} />
```
